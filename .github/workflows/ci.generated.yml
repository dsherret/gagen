# GENERATED by .github/workflows/ci.ts -- do not edit manually

name: ci
on:
  - push
  - pull_request
concurrency:
  group: "${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}"
  cancel-in-progress: true
jobs:
  deno:
    name: gagen
    if: 'github.event_name == ''push'' || !startsWith(github.event.pull_request.head.label, ''dsherret:'')'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - uses: denoland/setup-deno@667a34cdef165d8d2b2e98dde39547c9daac7282
        with:
          deno-version: canary
      - name: fmt
        run: deno fmt --check
      - name: lint
        run: deno lint
      - name: test
        run: deno test -A
      - name: lint ci
        run: deno run -A ./src/cli.ts --lint
  jsr:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - uses: denoland/setup-deno@667a34cdef165d8d2b2e98dde39547c9daac7282
        with:
          deno-version: canary
      - name: Publish to JSR on tag
        run: "deno run -A jsr:@david/publish-on-tag@0.2.0"
  npm:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - name: Get tag version
        id: get_tag_version
        if: 'startsWith(github.ref, ''refs/tags/'') && runner.os == ''Linux'''
        run: 'echo TAG_VERSION=${GITHUB_REF/refs\/tags\//} >> $GITHUB_OUTPUT'
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f
        with:
          node-version: 24.x
          registry-url: "https://registry.npmjs.org"
      - uses: denoland/setup-deno@667a34cdef165d8d2b2e98dde39547c9daac7282
        with:
          deno-version: canary
      - name: npm build
        run: "deno task build:npm ${{ steps.get_tag_version.outputs.TAG_VERSION }}"
      - name: npm publish
        if: 'startsWith(github.ref, ''refs/tags/'') && runner.os == ''Linux'''
        run: cd npm && npm publish

# gagen:pin actions/checkout@v6 = de0fac2e4500dabe0009e67214ff5f5447ce83dd
# gagen:pin actions/setup-node@v6 = 53b83947a5a98c8d113130e565377fae1a50d02f
# gagen:pin denoland/setup-deno@v2 = 667a34cdef165d8d2b2e98dde39547c9daac7282
